function lastCharacter(str) {
    if (str === "") {  
    return "undefined";
  }   
  // else if (str === typeOf 'number') { 
  //   return "undefined";
  // }
  // else { 
  //   return str.charAt(str.length-1);
  // }

}

 console.log(lastCharacter("hh"));